<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link rel="stylesheet" href="/css/output.css" />
  </head>
  <body class="bg-gray-100 flex items-center justify-center h-screen font-sans">
    <form method="POST" action="/login" class="bg-white shadow-lg rounded-lg p-8 w-96">
      <h2 class="text-2xl font-bold mb-6 text-center text-gray-800">
        User Login
      </h2>

      <% if (error) { %>
      <div class="bg-red-100 text-red-600 p-2 rounded mb-4">
        <%= error %>
      </div>
      <% } %>

      <label class="block mb-2 text-sm font-medium text-gray-700">Username</label>
      <input type="text" name="username"
        class="w-full border p-2 rounded mb-4 focus:ring-2 focus:ring-blue-500 focus:outline-none"
        required />

      <label class="block mb-2 text-sm font-medium text-gray-700">Password</label>
      <input type="password" name="password"
        class="w-full border p-2 rounded mb-6 focus:ring-2 focus:ring-blue-500 focus:outline-none"
        required />

      <button type="submit"
        class="w-full bg-blue-600 text-white py-2 rounded hover:bg-blue-700 transition duration-150">
        Login
      </button>
    </form>
  </body>
</html>
